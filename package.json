{
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && rm -rf dist/* && cp -r public/* dist/ && elm make src/Main.elm --output=dist/elm.js",
    "test": "elm-test"
  },
  "devDependencies": {
    "elm": "^0.19.1-6",
    "elm-test": "^0.19.1-revision17"
  }
}
