name: Build PocketBase Image

on:
  push:
    paths:
      - "packages/pb_server/pb_hooks/**"
      - "packages/pb_server/Dockerfile"
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v6

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: packages/pb_server
          push: true
          tags: ghcr.io/${{ github.repository }}/pocketbase:latest
