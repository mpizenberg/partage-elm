# PocketBase URL
# For local development: http://127.0.0.1:8090
# For production: URL of your deployed PocketBase instance (e.g., https://pocketbase.yourdomain.com)
PB_URL=http://127.0.0.1:8090

# PocketBase Admin Credentials (required for automatic collection setup)
# These credentials are used by the setup script to create collections
# Make sure to set these as environment variables in your Dokploy deployment
PB_ADMIN_EMAIL=admin@example.com
PB_ADMIN_PASSWORD=your-secure-password

# ============================================================
# Proof-of-Work Anti-Spam Configuration
# ============================================================
# PoW prevents spam account creation by requiring computational work.
# The secret is used for HMAC-signing challenges (stateless verification).
#
# IMPORTANT: Change this secret in production!
# ============================================================

# Secret key for HMAC-signing PoW challenges
# Use a long random string in production (e.g., generate with: openssl rand -base64 32)
POW_SECRET=partage-pow-secret-change-in-production
